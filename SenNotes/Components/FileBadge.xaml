<UserControl x:Class="SenNotes.Components.FileBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SenNotes.Components"
             mc:Ignorable="d"
             x:Name="Root"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converter="clr-namespace:SenNotes.Converter"
             d:DesignHeight="100" d:DesignWidth="200">
  <UserControl.Resources>
    <converter:FileTypeConverter x:Key="FiletypeConverter"/>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="{Binding Foreground, ElementName=Root}"/>
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{Binding Foreground, ElementName=Root}"/>
    </Style>
    <Style TargetType="md:PackIcon">
      <Setter Property="Foreground" Value="{Binding Foreground, ElementName=Root}"/>
    </Style>
  </UserControl.Resources>
  <Grid >
    <Button Click="ButtonBase_OnClick" Style="{StaticResource MaterialDesignFlatButton}">
      <StackPanel Orientation="Horizontal">
        <md:PackIcon Kind="{Binding FileSource.FileType, ElementName=Root, 
        Converter={StaticResource FiletypeConverter}}"/>
        <TextBlock  Text="{Binding FileSource.FileName, ElementName=Root}"/>
      </StackPanel>
    </Button>
  </Grid>
</UserControl>
